import React, { useState } from 'react';
import './App.css';

const App: React.FC = () => {
  const [recipes, setRecipes] = useState<string[]>([]);

  return <div>Soupwarts</div>;
};

export default App;
